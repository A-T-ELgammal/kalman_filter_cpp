{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "C++ Debugger",
      "command": "/usr/bin/g++", // Use the full path to g++ executable
      "args": [
        "-g",
        "-std=c++17",
        "${workspaceFolder}/kalman_filter/*.cpp",
        "-o",
        "${workspaceFolder}/kalman_filter/main"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Task generated by Debugger."
    }
  ],
  "version": "2.0.0"
}
